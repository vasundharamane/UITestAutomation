package com.task.automation.test;

import java.net.MalformedURLException;
import java.net.URL;

import org.openqa.selenium.Platform;
import org.openqa.selenium.remote.DesiredCapabilities;
import org.openqa.selenium.remote.RemoteWebDriver;
import org.testng.annotations.Test;

import com.task.automation.core.BaseTestCase;

public class chromeGrid extends BaseTestCase{
	@Test
	public static void chromegrid() throws MalformedURLException, InterruptedException {
//	DesiredCapabilities dcap = DesiredCapabilities.chrome();
//	dcap.setPlatform(Platform.LINUX);
//	dcap.setVersion("");
//	driver=new RemoteWebDriver(new URL(""),dcap);
//	driver.manage().window().maximize();
//	Thread.sleep(8000);
//	driver.get("https://www.google.com");
//

}}
